# IP Vault slides

Place confidential deck files here (not in `public/`).

Supported: `.png`, `.jpg`, `.jpeg`, `.webp`, `.pdf`

Name files in order, e.g. `slide-01.png`, `slide-02.png`, or one `deck.pdf`.
